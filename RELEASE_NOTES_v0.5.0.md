# Cos Light Studio 0.5.0

- 修复多材质网格合并丢失 PMX 材质分区：不再将全模型错误映射到脸部贴图；新增原始分区逐三角形回归检查，包括完整棚景及多次重建。
- 删除旧自制人物、拼接身体、假发、服装生成器及废弃编辑器；男性扫描只保留原始头部。
- 所有角色共用原色、18% 灰、雕塑、去高光漫反射、灰色亮面模式；保留贴图透明轮廓。新增无光照原色检查与黑白显示。
- 新增关闭全部灯并恢复黑棚的自定义起点；切模型保留灯光与相机参数；保存方案支持重命名。
- 摄影机画面支持焦段滚轮、WASD/QE、相机绕拍、模特转向和平移、头与四肢节点、手掌和手指骨骼旋转。
- 拖动时使用原色快速预览，松开恢复路径追踪。新增小包升级及完整操作说明。
- 方案格式 v4，兼容读取 v1–v3；旧自制身体不会因导入老存档而恢复。

下载仓库 `main` 的 ZIP 即可启动 Lee 头模。三个受限模型不随公开代码分发，只在用户本机导入。已有 0.4 个人版可复制素材到新版本，无需再下载 83 MB。

测试不等于实机验收：本次环境未完成 Windows GPU 画面与鼠标操作手感验证；没有提供伪装成 GPU 实测的静态宣传图。完整升级步骤与操作表见 UPDATE_GUIDE.zh-CN.md。
