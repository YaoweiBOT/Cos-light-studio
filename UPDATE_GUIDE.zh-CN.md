# 0.5 本地升级与操作说明

## 不用重下 83 MB：沿用自己已有的模型

1. 先在旧程序导出重要方案 JSON，然后关闭旧启动窗口。
2. 把 `cos-light-studio-source-v0.5.0.zip` 完整解压到**新文件夹**。旧文件夹留作备份，不要混用旧 JS 与新 JS。
3. 以下两种方式任选其一：
   - **已有三个原始模型 ZIP**：选中它们，拖到新文件夹的 `import-models-windows.cmd`。需要 Python 3.8+。也可逐个拖入。完整步骤见 `MODEL_IMPORT_GUIDE.zh-CN.md`。
   - **已有 0.4 个人版文件夹**：将旧文件夹的 `assets/MMD` 整个复制到新版的 `assets/MMD`；再将旧的 `dist/models/MMD` 整个复制到新版的 `dist/models/MMD`。两处都要复制，保留原始层级和作者说明。不要复制旧版 `dist/assets`（那是有问题的旧程序代码）。如果只有运行目录，优先改用原始 ZIP 导入。
4. 双击新目录的 `start-windows.cmd`，保留启动窗口。在 Chrome / Edge 按 Ctrl+Shift+R，左上角应显示 **LOCAL 0.5**。
5. 使用原来相同的 `http://127.0.0.1:5273/` 时，浏览器存档会自动沿用并迁移。不同浏览器或端口请导入之前导出的 JSON。

新程序缺少角色时会先显示 Lee 扫描；不代表它自动下载了模型。运行 Python 启动器不需要重新构建。手工复制两处模型后，Node 启动器可能重新检查构建；希望完全离线时可直接用 Python 启动器。

## 先确认模型颜色，再练灯

1. 画面下方选择 **耀嘉音**，表面选 **原始配色**。
2. 渲染选 **原色检查 · 无光照**。按「头部」或「半身」取景。深色头发和黑白服装应该使用各自贴图，而不是整个人都变成肤色。
3. 改回 **物理布光 · 路径追踪**，选「45° 面光」并等待采样。原色检查忽略灯光、曝光和白平衡；它不是布光结果，也不是原作者的完整 MMD Toon/SPA 渲染。
4. 切换 **18% 灰** 观察阴影，再切回原始配色。灰色模式保留透明轮廓，不应把睫毛、眼影、头发卡片变成不透明方块。

0.4 的错误发生在路径追踪场景合并：一个多材质 PMX 被当作一个材质区处理。以耀嘉音为例，17 个分区最终全部使用了脸部贴图。0.5 在合并后、构建 BVH 前恢复各材质分区，并逐三角形核验对应关系。没有修改你的原始 PMX 或 PNG。

## 主画面的鼠标与键盘

先点击摄影机画面，使其获得焦点。输入框、下拉框或对话框里不会触发相机移动。

| 操作 | 效果 |
| --- | --- |
| 滚轮向上 / 向下 | 增加 / 减少焦段；不是移动相机。Shift + 滚轮精调 |
| W / S | 相机与取景中心一起上升 / 下降，尽量保持原俯仰 |
| A / D | 相机与取景中心一起向场景左 / 右平移 |
| Q / E | 相机后退 / 前进；不改变焦段 |
| Shift + W/A/S/D/Q/E | 慢速精调 |
| 左键拖动空白处或人物 | 模特整体左右旋转；Ctrl + 拖动精调 |
| Shift + 左键拖动 | 平移模特；沿当前摄影机的水平轴与高度移动 |
| 右键拖动 | 相机绕人物基准位置左右绕拍、上下改变机位高度 |
| 中键拖动 | 平移相机与取景中心 |
| F | 主动适配取景：MMD 全身，Lee 头部 |
| Esc | 取消关节选择、停止键盘移动 |
| 1 / 2 / 3 | 选择主光 / 轮廓 / 顶光 |
| L | 单独看当前灯 / 退出单灯（旧版 S 已让给相机下降） |
| 空格 | 暂停 / 继续路径追踪 |

移动中临时显示**原色交互预览**，不计算灯光、景深、遮挡阴影或反弹；松开后再计算路径追踪。它用于流畅定位，不用于判断正在变化的灯光。这里的横移以场景水平 X 轴为基准，绕拍角度也可在相机面板精确输入。

## 头、手掌、手指和四肢节点

打开画面下方「关节节点」。节点只来自当前角色实际存在的骨骼，不生成替代肢体。节点按“人物正对镜头时的画面左 / 右”命名；转身后左右标签不交换。节点是编辑辅助，会透过人物显示，不表示真实可见表面。

1. **先定姿势**：选择自然站立、叉腰、招手等起点，或通过姿态图调整。
2. **手脚 IK**：拖菱形手腕 / 脚踝，在摄影机平面内移动；Alt + 拖动上下调远近。超出可达距离会收回到肢体长度上限。IK 编辑会清除细调旋转，避免肘腕同时受两种相反约束。
3. **头部**：拖头节点调左右朝向和俯仰；下方也会出现角度输入。
4. **其他关节**：选择上臂、肘、膝、颈、上半身等节点，拖动调骨骼局部 X/Y 旋转；Alt + 拖动调 Z 扭转。选中后可以精确输入三轴角度。
5. **手指**：层级切到「手掌 / 手指精细层」，先把画面放大，再点掌部或各指节。重叠或在画面外的节点也可通过下拉菜单选择。顺序建议：四肢 IK → 手掌 → 手指。
6. 「重置此关节」清除该关节旋转；如果选中的是 IK 端点，则恢复自然站姿的四肢设置。

目前不含 MMD Bullet 物理、裙摆碰撞、表情 morph、VMD 动作播放或生理关节约束。局部轴来自原骨骼，不保证每个角色的 X 都是同一个人体方向。长发、袖子、裙摆可能穿插；不要据此判断真实衣服的受力。仪玄长袖仍用静态下垂近似。

## 看光、清空与保存

| 功能 | 用途 |
| --- | --- |
| 原始配色 · 物理受光 | 看服装和头发的颜色与高光；贴图可能自带美术明暗 |
| 18% 灰 · 纯漫反射 | 线性反射率 0.18、无镜面高光，用于比较照明；不是保证屏幕像素为 18% |
| 中性雕塑 | 去掉材质配色干扰，观察面部结构与光型 |
| 原色漫反射 · 去高光 | 保留贴图，关闭镜面分量 |
| 灰色亮面 | 看光源形状、位置与高光覆盖 |
| 相机面板 → 黑白亮度 | 去掉色彩干扰，比较明暗 |
| 原色检查 | 不受灯光、曝光和白平衡影响，核对模型贴图；不能当作布光效果 |

「清空灯光 · 自定义」会关闭所有灯、退出单灯查看、移除两块板并恢复黑棚墙地面参数。相机、模型、道具和看光方式保留。此时物理画面全黑是正常的，手动开主光开始；也可点「45° 面光」恢复单灯起点。

切模型只切人物及其适配姿势，不重置灯光、相机、曝光、白平衡、道具、墙面或表面模式；镜头可能暂时拍不到新人物，按 F 或取景按钮调整。灯具的瞄准基点仍会随人物位置和坐姿高度变化，灯心位置与瞄准偏移参数不重置。

已保存方案右侧「✎」可重命名，只改名称，不改方案参数。自动保存与「保存方案」不同：后者会新增一个独立快照。相同名称允许并存。

## 验证与许可边界

已做本地数值、原始 PMX、骨骼、材质分区、状态迁移、打包和启动检查。**当前测试浏览器未能打开本地服务，尚未完成 Windows 显卡上的实际画面、流畅度与鼠标手感验收。** 旧 CPU 外观图不是本版 GPU 实测图，也不能证明 GPU 最终画面已经正确。

新包不包含三个受限 MMD，可沿用你自己原来持有的素材。模型原说明要求禁止二次配布；不要上传 PMX、贴图、材质、转换后的 GLB 或含人物素材的个人包。分享代码只分享 source 包，模型获取与使用遵守原作者说明。详见 `MODEL_IMPORT_GUIDE.zh-CN.md` 与 `THIRD_PARTY_NOTICES.md`。发布到 GitHub 时使用独立的 `release/v0.5.0` 分支，仅包含公开程序与说明，保留现有主分支和其他开发分支。
